# CLAUDE.md — Rhadix projectgeheugen

Dit bestand wordt automatisch gelezen bij elke nieuwe sessie. Het bevat alle projectkennis zodat er geen context verloren gaat bij restarts.

**Instructie voor Claude:** Lees dit bestand aan het begin van elke sessie. Werk de sessie-log bij aan het einde van elke sessie met een korte samenvatting van wat er gedaan is, en commit de update naar main + staging.

---

## Project

**Rhadix Datavalidatie** — een KIK-V / ZIB-validatietool voor zorginstellingen.
- **Repos:**
  - https://github.com/Rhadix2026/Rhadix-datavalidatie (deze app)
  - https://github.com/Rhadix2026/rhadix-uitvraag
  - https://github.com/Rhadix2026/Rhadix-datastation
- **Stack:** FastAPI (Python) backend, React/Vite frontend, PostgreSQL, Docker
- **Server:** `46.224.224.26` (Hetzner VPS, Ubuntu, 4GB RAM, 2GB swap)
- **SSH:** `ssh root@46.224.224.26`

---

## Git-credentials (lokaal)

De git-credentials staan opgeslagen in:
```
/Users/renehouwen/Downloads/rhadix-git/.git/config
```
Token: `<zie .git/config in rhadix-git repo>`

**Let op:** Bij git-operaties vanuit de sandbox altijd klonen naar /tmp/ (niet naar de gemounte map) vanwege lock-file problemen:
```bash
git clone https://<token>@github.com/Rhadix2026/Rhadix-datavalidatie.git /tmp/rhadix-work
cd /tmp/rhadix-work
git config user.email "rhadix@rhadix2026.nl"
git config user.name "Rhadix2026"
```

---

## Branch-strategie & deployen

| Branch | Omgeving | Hoe deployen |
|--------|----------|--------------|
| `main` | geen auto-deploy | Alleen via versie-tag |
| `staging` | Staging (poort 5175/8011) | Push naar staging = automatisch |
| `v*.*.*` tag op main | Productie (poort 5174/8010) | Tag aanmaken, GitHub Actions, handmatige goedkeuring |

### Na elke wijziging op main ook naar staging pushen:
```bash
git checkout staging && git merge main --no-edit && git push origin staging && git checkout main
```

### Nieuwe productie-release:
```bash
git tag v1.5.X && git push origin v1.5.X
```

**Huidige versie:** v1.8.1

---

## Server — handmatige herstelcommando's

Als productie down is:
```bash
ssh root@46.224.224.26
cd /opt/rhadix-app
docker compose -f docker-compose.prod.yml --env-file .env.production up -d
```

Inhoud .env.production als die ontbreekt:
```
GHCR_ORG=rhadix2026
DB_PASSWORD=<zie PROD_DB_PASSWORD in GitHub Secrets>
JWT_SECRET_KEY=<zie PROD_JWT_SECRET_KEY in GitHub Secrets>
RHADIX_LICENSE_KEY=<zie PROD_LICENSE_KEY in GitHub Secrets>
```

---

## Server-configuratie (stabiel)

- Docker on boot: `systemctl is-enabled docker` → enabled ✓
- Restart policy: `restart: always` op alle containers ✓
- Swap: 2GB `/swapfile` — controleer na reboot of swap in /etc/fstab staat!
- Geen OOM-history

---

## Codebase — belangrijke bestanden

| Bestand | Inhoud |
|---------|--------|
| `frontend/src/components/UI.jsx` | Nav, RhadixLogo, TreeDecoration componenten |
| `frontend/src/pages/Landing.jsx` | Landingspagina |
| `frontend/src/pages/LoginScreen.jsx` | Loginpagina |
| `frontend/src/App.jsx` | Routing + state (`step`) |
| `frontend/src/index.css` | CSS-variabelen (`--blue-hero`, `--blue-dark`, etc.) |
| `frontend/public/rhadix-logo.jpg` | Brand logo JPG (68KB, donkere achtergrond) |
| `frontend/public/rhadix-boom.jpg` | Brand boom JPG (84KB, donkere achtergrond) |
| `landing/index.html` | Marketing website (rhadix.nl) — apart van de Vite app |

---

## Bekende issues & oplossingen

- **Git lock-files in sandbox:** altijd naar `/tmp/` klonen, nooit naar gemounte Downloads-map
- **Staging toont oude versie:** pushes naar `main` deployen NIET naar staging — altijd handmatig mergen naar `staging` branch na een main-push
- **Browser-cache (immutable assets):** na logo/asset-updates altijd hard refresh `Cmd+Shift+R`
- **Auth-bootstrap (was destructieve testopzet):** bij startup wordt `admin@rhadix.nl` **niet-destructief** geborgd (aangemaakt als die ontbreekt; nooit `TRUNCATE` van users). `AUTH_RESET=0` slaat het over. Wachtwoord stond hardcoded en is gelekt — bij gelegenheid wijzigen.
- **AFAS-import JSON:** de import-kant accepteert sinds 2026-06-17 ook AFAS GetConnector JSON (`{skip,take,rows}`), naast XML. JSON- en XML-parser leveren identieke rijen (pariteit getest).
- **Bad gateway productie:** controleer of `.env.production` aanwezig is én `GHCR_ORG=rhadix2026` erin staat

---

## ✅ SSO op productie — OPGELOST (2026-06-24)

Eén login over alle 4 apps werkt: token op app.rhadix.nl is **RS256** (kid suresync-id-1); uitvraag/datastation/crm geven 200 op `/api/auth/me` met het centrale token. **Oorzaak was** lege `JWT_PRIVATE_KEY` (GitHub-secret `PROD_JWT_PRIVATE_KEY` kwam leeg binnen). **Fix:** privésleutel als one-liner base64 in `/opt/rhadix-app/jwt_private.env` (persistent, chmod 600) + `deploy-production.yml` injecteert dat bestand bij elke deploy (i.p.v. de lege secret). Publieke sleutel staat als default in de 4 prod-composes. priv.pem-backup: bewaar veilig.

### (historie) OPENSTAAND was: SSO op productie afmaken

**Status:** CRM logt wél via SSO in, Uitvraag/Datastation/Datavalidatie nog niet.
**Oorzaak (bevestigd op server):** op datavalidatie-prod is **`JWT_PRIVATE_KEY` leeg**
(`/opt/rhadix-app/.env.production` regel 13 = `JWT_PRIVATE_KEY=` zonder waarde; in de
container lengte 1). `JWT_PUBLIC_KEY` staat er wél (605 tekens). Daardoor tekent de uitgever
met **HS256** i.p.v. RS256 → de resource-apps wijzen het centrale token af (401).
De GitHub-secret `PROD_JWT_PRIVATE_KEY` kwam bij de deploy **leeg** binnen (verkeerd/leeg opgeslagen).

**Fix (kies één):**
1. *Server-direct (snel):* op `46.224.224.26`, in `/opt/rhadix-app`:
   `base64 < /tmp/priv.pem | tr -d '\n'` → in `.env.production` als `JWT_PRIVATE_KEY=<oneliner>` zetten
   (regel vervangen), dan `docker compose -p <PROJ> -f docker-compose.prod.yml --env-file .env.production up -d --force-recreate backend`.
   (PROJ via `docker inspect <backend-container> --format '{{ index .Config.Labels "com.docker.compose.project"}}'`.)
   Werkt alleen als `/tmp/priv.pem` er nog staat.
2. *Vers paar:* nieuw RS256-paar genereren, privé als `JWT_PRIVATE_KEY` op de server + als secret
   `PROD_JWT_PRIVATE_KEY`, en de **nieuwe PUBLIEK** (base64) herbakken als default
   `JWT_PUBLIC_KEY`/`CENTRAL_JWT_PUBLIC_KEY` in de vier prod-composes → 4 mini-releases.
3. *Durabel:* zorg dat secret `PROD_JWT_PRIVATE_KEY` (repo Rhadix-datavalidatie) de **one-liner base64**
   is (begint met `LS0tLS1CRUdJTiBQUklW`, géén enters), dan redeploy → blijft staan bij volgende deploys.

**Verificatie na fix:** login app.rhadix.nl → token-header moet `RS256` zijn → `GET /api/auth/me`
met dat token op uitvraag/datastation/crm geeft 200 + de user (JIT).

**SSO-bedrading (staat al goed):** uitgever = datavalidatie (`JWT_PRIVATE_KEY`/`JWT_PUBLIC_KEY`/
`JWT_ISSUER=suresync-id`/`SSO_COOKIE_DOMAIN=.rhadix.nl`); resource-apps = `CENTRAL_JWT_PUBLIC_KEY`
(prod-publieke sleutel als default in compose) + `CENTRAL_JWT_ISSUER=suresync-id`. Cookie `rhadix_sso`
op `.rhadix.nl` wordt al gezet.

**Veiligheid:** een `ghp_`-GitHub-token + admin-wachtwoorden stonden in platte tekst zichtbaar in een
notitiebestand — token intrekken/roteren.


## Sessie-log

| Datum | Versie | Wijziging |
|-------|--------|-----------|
| 2026-08-06 | v1.9.8 | PRODUCTIE-RELEASE — beheer-UX + fixes. (1) **Rol RHADIX_ADMIN (+ RSO_ADMIN) toewijsbaar** in gebruiker aanmaken/bewerken (AdminDashboard) -> tweede Rhadix-beheerder mogelijk (geen single point of failure); backend stond het al toe, alleen menu-opties ontbraken. Laatste-actieve-admin-bescherming blijft. (2) **Terug vanuit Beheer/Org-beheer -> portaal** (was: bronkeuze 'systems'). (3) Nav-knop **'Platform' -> 'Platform-dashboard'** (dubbele 'Platform'-labels in de balk weg; '▦ Platform' blijft de portaal-knop). (4) **Crashfix**: `OrgDashboard` en `PlatformDashboard` misten de `NavBack`-import -> render liep vast bij openen 'Organisatie'/'Platform-dashboard' (ErrorBoundary). Latente bug, los van de rol-uitbreiding. Tests: user-mgmt promote-to-admin (2). Tag v1.9.8 -> prod-deploy (handmatige goedkeuring). |
| 2026-08-06 | v1.9.7 | PRODUCTIE-RELEASE — beheer-verfijningen. (1) **App-toewijs-lijst = portaal-tegels** (Datavalidatie/Uitvraag/Datastation/CRM/Reconciliation Engine); validatie-sub-modules (KIK-V/ZIB/Algemeen Validator) verborgen uit de toewijs-lijst (blijven bestaan voor toegang). CRM (`rhadix-crm`) + `reconciliation-engine` geseed; oude losse `reconciliation`-rij op inactief (dedup). (2) **Validatie-toegang losgekoppeld** van de per-standaard validator-slugs -> volgt nu het product **Datavalidatie** (legacy-slug blijft fallback); zowel frontend-lock (SelectSystems) als backend (`validate/upload`). (3) **Org-brede app-toewijzing werkt door naar gebruikers**: `_app_slugs_for` = TenantApplication ∪ UserApplication, zodat een toewijzing op organisatieniveau alle gebruikers (incl. RSO_ADMIN) toegang geeft — loste op dat een RSO-beheerder niet kon valideren. (4) **Licenties bewerken + verwijderen**: nieuw `DELETE /admin/licenses/{id}` (koppelt tenant_app.license_id->NULL) + Bewerken/Verwijderen in de Licenties-tab. Tests: org-level-access + license-delete (5), rso-picker (16), branding (12) groen. Tag v1.9.7 -> prod-deploy (handmatige goedkeuring). |
| 2026-08-06 | v1.9.6 | PRODUCTIE-RELEASE — RSO-niveau + eigen look-and-feel per niveau. Bundelt twee features die op staging getest zijn. **(1) RSO-niveau (samenwerkingsorganisatie):** nieuwe rol `RSO_ADMIN` tussen RHADIX_ADMIN en ORG_ADMIN; `Tenant.tenant_type` ('ORG'/'RSO'/'PLATFORM') + `parent_tenant_id` (ouder-kind, SET NULL) via startup-vangnet (ALTER TABLE + `ALTER TYPE userrole ADD VALUE` in AUTOCOMMIT). RHADIX_ADMIN kan een RSO-tenant aanmaken (eerste beheerder krijgt RSO_ADMIN) en een organisatie onder een RSO plaatsen. Nieuwe gescoopte router `/api/rso` (eigen RSO + kinderen): organisaties aanmaken, gebruikers aanmaken/bewerken/deactiveren/reset-pw, apps toewijzen/intrekken; strikt gescoped, laatste-actieve-RSO_ADMIN-beschermd; DEFINITIEF verwijderen blijft RHADIX_ADMIN. Frontend `RsoDashboard` + 'Beheer (RSO)'-knop + routing. 15 tests. **(2) Look-and-feel per niveau:** `TenantBranding` (preset/primaire+accentkleur/wordmerk/logo-bytes) geborgd in DB; effectieve-branding-resolver met overerving org->RSO->platform->Rhadix-default; admin-endpoints (RHADIX_ADMIN) GET/PUT/DELETE branding + logo upload/serve (`/api/branding/{id}/logo`, publiek, cache-busting); `/auth/me` levert effectieve branding; frontend `BrandingContext` zet CSS-variabelen + toont eigen logo/wordmerk; AdminDashboard 'Look & feel'-editor per organisatie (preset/kleuren/wordmerk/logo + preview). Alleen RHADIX_ADMIN stelt in; andere niveaus erven. 12 tests. Tag v1.9.6 -> prod-deploy (handmatige goedkeuring). |
| 2026-08-06 | v1.9.5 | PRODUCTIE-RELEASE — organisatie-/gebruikersverwijdering voor RHADIX_ADMIN (zie 2026-08-04-entry) live op prod; incl. api-regressiefix (stale lokale api.js miste listProfiles e.a.). |
| 2026-08-04 | — | BEHEER/LICENTIE — organisatie- en gebruikersverwijdering voor RHADIX_ADMIN (staging). Op verzoek: admin kon nog geen volledige organisaties (incl. gebruikers) verwijderen. Toegevoegd, veilig-tweetraps: (1) **user** delete/deactivate kreeg laatste-actieve-RHADIX_ADMIN-bescherming (bovenop bestaande self-guard). (2) **organisatie**: `GET /admin/tenants/{id}/impact` (telt gebruikers/licenties/app-koppelingen/taken/scans), `PATCH /admin/tenants/{id}/deactivate` (omkeerbaar; zet alle gebruikers mee op dezelfde status; eigen tenant geblokkeerd), `DELETE /admin/tenants/{id}` (hard, cascade users/licenties/tenant_apps/taken; **scans blijven bewaard maar tenant_id->NULL** als audittrail; vereist getypte organisatienaam-bevestiging; eigen tenant geblokkeerd). Frontend AdminDashboard: per org Deactiveren/Activeren + Verwijderen-knop met impact-modal + naam-bevestiging; api.js-helpers. 15 nieuwe backend-tests groen; auth/beheer (103) + user-mgmt (39) ongewijzigd groen; frontend-build schoon. |
| 2026-07-30 | v1.8.7 | PROD-HOTFIXES na v1.8.4-release + PROFIEL-LAAG-BUGS opgelost. Na livegang van de profiel-laag als standaard kwamen op prod drie dingen boven: (1) **Dashboards 401** — UserDashboard/OrgDashboard/PlatformDashboard stuurden `Authorization: Bearer null` bij cookie-inlog (SSO zonder token in geheugen) -> 401; nu alleen de header meesturen als er een token is (anders cookie-fallback). (2) **Render-crash** (top-level ErrorBoundary) -> AlgemeenDashboard FileCard defensief geguard (result.issues/result.rows). (3) **Valse 'verplicht veld ontbreekt'** in de KIK-V-benchmark: `profile_issues` gaf `to_canonical` de generieke naam 'upload', waardoor de bronherkenning o.a. Profit_Employees (medewerker) als **werkovereenkomst** classificeerde -> geboortedatum e.a. onterecht als ontbrekend. Fix: echte `fi['filename']` doorgeven. Getest op echte AFAS-export: Employees viel om (nu correct medewerker, DateOfBirth->geboortedatum mapt; toont terecht kapotte datumwaarden `199-02-23`); Illness/Timetable werden al via headers goed herkend. (4) **detect_schema** herkent nu ook de Engelse AFAS-naam 'Functions' -> recordtype functie (Werkgevers/Organigram hebben geen KIK-V-recordtype, blijven terecht buiten de benchmark). Belangrijk inzicht: datakwaliteit (stap 1, validate_algemeen) en de KIK-V-benchmark (profiel-laag) verschillen bewust — stap 1 = kwaliteit van aangeleverde velden, benchmark = KIK-V-conformiteit incl. verplichte-veld-aanwezigheid (streng, zo gekozen). Tags v1.8.5/v1.8.6 (los) opgeruimd; v1.8.7 bundelt alles. |
| 2026-07-24 | v1.8.4 | MULTI-BRON VALIDATIE + PROFIEL-LAAG ALS STANDAARD (prod=staging). **Multi-bron:** 'kies je bron' meervoudig (aanvinken); bij 2+ bronnen een nieuwe MultiSourceValidatie-pagina met per-bron uploadzones + happy-flow-knop (verdeelt de voorbeeldset over AFAS HRM/Financieel/Nedap ONS), per-bron generieke validatie, **cross-checks tussen bronnen** (uit reconciliatie-indicatoren: medewerker/werkovereenkomst/verzuim AFAS vs ONS, uniek<=totaal, financieel+HR aanwezig), en een **KIK-V/ZIB-benchmark** (validate_files over alle bronnen -> score+per-recordtype). **Rapportage & opvolging** gekoppeld: KIK-V readiness/beschikbaarheid/management PDF op run_id + herbruikbare MaakTakenButton (bevindingen->taken, toegewezen aan org-gebruiker met mail) met **selectie** (checkbox per bevinding + alles-selecteren). **Doelarchitectuur Stap 2 rijker KIK-V-profiel:** cross-field regels naar de profiel-laag met verdict-pariteit — datumvolgorde (2.6, end_before_start), conditioneel verplicht (2.7, einddatum bij tijdelijk contract), placeholder+bereik (2.8, 99999/invalid_pct). Pariteitssweep vastgelegd; resterende legacy-only meldingen gedocumenteerd. **Omschakeling (2.4):** validate_files draait via use_profiles() op de profiel-laag; nu **default AAN** (RHADIX_USE_PROFILES=0 = noodrem). **Merge:** staging bevatte parallel werk (algemeen-scoring/prescan-JSON, benchmark-uitklapbaar, reconciliation-JSON + tests) — samengevoegd i.p.v. overschreven (schone auto-merge, alleen validator.py overlapte). main+staging gelijk op 1336860; tag v1.8.3 (miste parallel werk) verwijderd, v1.8.4 = prod. LET OP: er werkt een tweede sessie aan dezelfde repo. |
| 2026-07-24 | v1.8.2 · u v0.7.6 · ds v1.0.6 · crm v0.2.3 | PLATFORM UNIFORMERING + SSO-HERSTEL naar productie. **Navigatie/look-and-feel gelijkgetrokken** over de 4 apps: uniforme navy-balk met Rhadix-logo (wordmerken/SVG-marks eruit; logo-asset naar ds+crm gekopieerd), '← Terug' in de balk, '▦ Platform'-knop (naar portaal 'kies een applicatie'; env-afhankelijk app(-staging).rhadix.nl), Uitloggen. Reconciliation Engine kreeg de gedeelde Nav-balk (was zonder). CRM: tabs behouden in navy-stijl, SureSync-toggle eruit, Terug via tab-historie + fallback browserhistorie. Navy is nu vaste standaard; KIK-V blijft niet meer plakken (alleen via ?brand=kikv). **SSO gerepareerd:** CRM-bootstrap riep getMe alleen met opgeslagen token (nooit cookie) → nu altijd; Uitvraag/Datastation hadden geen mount-bootstrap → toegevoegd; **DV leest nu zelf het centrale rhadix_sso-cookie** (cookie-fallback in get_current_user/get_optional_user/require_app_access) zodat 'Terug naar platform' auto-inlogt op het portaal. **Staging-SSO 401 opgelost:** uitgever tekende met HS256 want STAGING_JWT_PRIVATE_KEY-secret was leeg → vers RS256-paar, publieke sleutel in de 4 staging-composes, private als repo-secret STAGING_JWT_PRIVATE_KEY (repository secret, niet environment). **Cookie-scheiding staging↔prod:** cookienaam env-gestuurd (SSO_COOKIE_NAME); staging=rhadix_sso_staging, prod blijft rhadix_sso. **nginx:** index.html met Cache-Control no-cache (voorkomt stale-bundle-na-deploy; hashed assets blijven immutable). **Versie-indicator** in de balk via VITE_APP_VERSION (prod=git-tag, staging=staging-<buildnr>). 32 auth-tests groen incl. nieuwe cookie-fallback-test. Tags v1.8.2/v0.7.6/v1.0.6/v0.2.3 → prod-deploys goedgekeurd en getest. |
| 2026-07-14 | v1.8.1 | PROD-FIXES na v1.8.0-test. (1) Reconciliatie-tegel in 'Kies een applicatie' werkte niet: `onReconciliation` werd niet aan AppPortal doorgegeven -> nu wel (gegate op app-slug); losse Reconciliation-knop uit Dashboard verwijderd. (2) SSO-diagnose: backend werkt volledig op prod (login = RS256-token, alle resource-apps geven 200 op /api/auth/me via het rhadix_sso-cookie) — géén HS256/jwt_private.env-probleem. Oorzaak was puur frontend: Uitvraag+Datastation riepen `/auth/me` nooit aan bij mount -> loginscherm ondanks geldig cookie. SSO-bootstrap toegevoegd (Uitvraag v0.7.5, Datastation v1.0.5; CRM had 'm al). NB: prod-admin is hernoemd naar r.houwen10265@gmail.com (admin@rhadix.nl bestaat niet meer). LES: eerdere staging-reset kwam van lokale `git push origin main:staging --force`. |
| 2026-07-14 | v1.8.0 | PRODUCTIE-RELEASE. Grote sprong vanaf v1.7.2: Stap 1 doelarchitectuur (canoniek rijmodel + ingest-pipeline), Stap 2 (generieke controls + declaratieve profielen t/m slice 2.5, Algemeen issue-pariteit + ZIB/KIK-V/uniciteit verdict-pariteit — additief, nog niet op endpoints aangesloten), auth-e-mailflows (reset/invite/verify, mail default UIT), reconciliatie-tegel in AppPortal, KIK-V-demo-skin (op prod gegate via VITE_RHADIX_ENV=production -> blijft Rhadix), SureSync verwijderd, happy-flow twin-voorbeeldset (knop blijft ook op prod). 245 tests groen. `main` fast-forward naar staging; tag v1.8.0 -> prod-deploy via GitHub Actions (handmatige goedkeuring). Ook Uitvraag + Datastation releasen (SureSync eruit + gegate KIK-V-skin). |
| 2026-07-14 | — | KIK-V-thema compleet doorgevoerd + SureSync verwijderd op alle drie de apps (staging). SureSync-merk/palet/toggles/decoratie weg (brand.js/index.css/AppPortal/LoginScreen/PlatformLanding/Landing/UI/Brand.jsx); de white-label-toggle is nu een KIK-V-schakelaar. KIK-V blijft schakelbaar via `?brand=kikv` en loopt bij portal-navigatie mee naar de sub-apps. Brand-blauwe hardcoded kleuren -> echo-variabelen `--k-blue*` (default = blauw ongewijzigd, onder KIK-V roze) zodat de skin nu ook doorwerkt in componenten die eerder hardcoded blauw waren. Builds schoon, alle drie de staging-deploys groen. NB: de 4 Profit-XML-bestanden voor de happy-flow twin-set zijn gegenereerd + geverifieerd (11/11 clean) maar nog niet toegevoegd — staan klaar in /tmp/kikv-xml. |
| 2026-07-13 | — | KIK-V HAPPY-FLOW IMPLEMENTATIE-TWIN VOORBEELDSET (staging). 15 synthetische, consistente happy-flow CSV's (40 medewerkers die terugkomen in medewerker/werkovereenkomst/verzuim/timetable-logica + finance/vestiging/functie/client/contract; geldige elfproef-BSN's; komma-gescheiden zoals de engine verwacht). Geverifieerd tegen de échte reken-engine: alle 24 CSV-indicatoren rekenen clean door (40 mw, 36 actieve werkovereenkomsten, 14 verzuim, 60 cliënten, 90 boekingen ↔ 15 grootboekrubrieken), 0 fouten. AVG-proof want synthetisch (past bij 'geen echte cliëntgegevens'). In `frontend/public/kikv-voorbeeldset/*.csv` + `kikv-voorbeeldset.zip`. Happy-flow-tab (ReconciliationDashboard): knop 'Laad KIK-V voorbeeldset' (één-klik, hergebruikt het bestaande `/happy-flow/batch`-endpoint) + 'Download voorbeeldset (ZIP)'. Backend ongewijzigd. Deploy groen. VOLGENDE: Profit-XML-bestanden aan de set toevoegen; demo-script end-to-end; evt. Uitvraag/Datastation twin-data. |
| 2026-07-13 | — | KIK-V DEMO-SKIN (staging, schakelbaar) voor de KIK-V implementatie-twin. Vraag vanuit programma KIK-V: variant van DV/Uitvraag/Datastation in KIK-V look-and-feel met happy-flow als 'digitale implementatie-twin'; demo volgende week. Aanpak: géén aparte omgeving nodig — schakelbare `kikv`-skin op de bestaande merk-laag. `frontend/public/kikv-logo.png` (roze stempel, transparant, uit brandbook; hi-res drop-in vervangbaar). `brand.js`: merk `kikv` + `getInitialBrand` staat alt-skins alleen buiten productie toe (productie blijft Rhadix). `index.css` `:root[data-brand=kikv]` met KIK-V werkpalet (blauw #2e6896, geel #fad94c, roze #ba2a5e + tints, Verdana) + geel->roze accentbalk. Activeren: `?brand=kikv`; default = groene staging (terug-naar-groen is gratis, staging blijft voor ons eigen werk bruikbaar). Build groen, staging-deploy groen. VOLGENDE: skin repliceren naar Uitvraag + Datastation (eigen repos), happy-flow twin-seed (voorgeladen ideale route), demo-script end-to-end DV->Uitvraag->Datastation. Als KIK-V het echt wil: aparte VPS opzetten. |
| 2026-07-13 | — | DOELARCHITECTUUR Stap 2 slice 2.5 (staging): rijker profiel — uniciteits-regel. `controls.run_unique` signaleert dubbele niet-lege waarden (count = betrokken rijen). `Profile` kreeg een `unique`-lijst; `profile_from_kikv` vult die met de primaire identifier (`_KIKV_UNIQUE`, medewerker->personeelsnummer). `run_profile` draait de uniciteitscheck naast de veld-checks. VERDICT-PARITEIT: run_profile unique-count == KIK-V `run_file_checks` `dup_id` count. 3 tests. Suite 245 groen, staging-deploy groen. Eerste relationele/bespoke KIK-V-regel is nu ook generiek. VOLGENDE: meer bespoke KIK-V-regels (cross-field bv. einddatum>=startdatum, berekeningsregels) naar het rijkere profiel; daarna endpoints omschakelen. |
| 2026-07-13 | — | BRANCH-HERSTEL. `staging` en `main` bleken teruggezet (force-push) naar een Stap-0-basis met 4 nieuwe 'Reconciliation Engine tegel in AppPortal'-commits (tip c8e2761), waardoor de auth-e-mailflows én de hele Stap 1 (canoniek model) + Stap 2 (generieke controls + profielen, tot slice 2.4, tip 2239392) van de branch waren verdwenen. Hersteld via GitHub-API: commit 2239392 bestond nog -> recovery-branch `recovery-stap12` aangemaakt -> reconciliatie-tegel (c8e2761) erin gemerged op `integratie-stap12-recon` (1 conflict in AppPortal.jsx: signatuur kreeg zowel `onTasks` als `onReconciliation`) -> 242 tests groen + frontend-build schoon. `staging` en `main` FAST-FORWARD naar de integratie (147739b): niets overschreven, reconciliatie-tegel behouden, e-mailflows + Stap 1/2 terug. Staging-deploy groen. Backup-branches op GitHub: `recovery-stap12`, `integratie-stap12-recon`. LES: de reset kwam waarschijnlijk van een lokale kopie/collega die op de oude lijn werkte; check bij parallel werk of de remote niet is teruggezet. VOLGENDE: doorgaan met rijker profiel (duplicaat-check, verdict-pariteit KIK-V dup_id). |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 2 slice 2.4 (staging): KIK-V veld-niveau naar profiel. `control_profiles.profile_from_kikv` uit `KIKV_REFERENCE` (required_cols, datumvelden=naam met 'datum', `overeenkomsttype`-codelijst via allowed_types=CONTRACTTYPE_VALUES). Verdict-pariteit: generieke date-check == `is_date`; generieke codelijst == KIK-V `value.lower().strip() in allowed` (alle codelijstwaarden zijn lowercase, dus exact gelijk). `run_profile` signaleert nu ongeldig contracttype én ongeldige startdatum op een KIK-V-bestand. De bespoke KIK-V-regels (dubbele identifiers, cross-field, berekeningsregels) BLIJVEN in `run_file_checks` en horen bij een rijker profiel (relationele/berekeningsregels) — latere stap, sluit aan op de reconciliatie-engine. 4 tests. Suite 242 groen, staging-deploy groen. Nu draaien Algemeen (issue-pariteit), ZIB en KIK-V (verdict-pariteit) alle drie op de generieke laag. VOLGENDE: rijker profiel voor de bespoke regels, of 2.x omschakelen endpoints. |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 2 slice 2.3 (staging): ZIB naar profiel. `controls` uitgebreid met codelijst-check + numeric-alias (check_value/run_column krijgen `allowed`). `control_profiles.profile_from_zib` bouwt een profiel uit `ZIB_FIELD_RULES` (required/type/allowed_values -> check). ZIB gebruikt eigen check-implementaties/meldingen, dus VERDICT-pariteit i.p.v. issue-pariteit: de generieke control keurt per waarde hetzelfde goed/af als ZIB `_validate_value` (bsn/date/code/numeric) — incl. bevestiging dat beide BSN-elfproef-implementaties het eens zijn. `run_profile` werkt nu ook op ZIB (verkeerde BSN wordt gesignaleerd). 3 tests. Suite 238 groen, staging-deploy groen. String-velden met bespoke `detect_format` en KIK-V `run_file_checks` (bespoke) volgen als aparte slice. VOLGENDE: KIK-V naar profiel, daarna 2.4 omschakelen op endpoints. |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 2 slice 2.2 (staging): declaratief profiel + runner. Nieuw `app/services/control_profiles.py` met `Profile` (required/optional concept->check), `profile_from_algemeen_template` en `run_profile` die de generieke controls (Laag 2) op een `CanonicalFile` (Laag 1) toepast -> uniforme bevindingen. PARITEIT BEWEZEN: `run_profile` == `validate_algemeen` (ontbrekend veld + invalid_{type}, severity + count) op een testbestand. 3 tests. Suite 235 groen, staging-deploy groen. Hiermee staat het bewijs dat de generieke laag het Algemeen-pad exact reproduceert. VOLGENDE: slice 2.3 (KIK-V & ZIB naar profielen, pariteit per recordtype), daarna 2.4 omschakelen op de endpoints. |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 2 GESTART (staging) + plan. Plan `STAP2_implementatieplan.md`: Laag 2 (generieke controles op canoniek model), Laag 3 (KIK-V/ZIB als declaratieve profielen), plus expliciete plek voor **rapportage** (eigen overzichts-tegel: history × report-types, JSON/PDF via bestaande endpoints) en **reconciliatie** (blijft aparte app-slug-gated module, gaat het canonieke model als bron gebruiken). Slice 2.1: `app/services/controls.py` — generieke checks (required/date/bsn/codelijst/formaat) + uniforme `Finding` + kolomrunner op het canonieke model; hergebruikt `algemeen_validator.VALIDATORS` en `column_values` leest de genormaliseerde `cell.value`. Pariteit per constructie (check_value == VALIDATORS). Nog niet aangesloten op endpoints. 5 tests. Suite 232 groen, staging-deploy groen. VOLGENDE: slice 2.2 (declaratief profielschema + eerste profiel, pariteit), daarna KIK-V/ZIB naar profielen en omschakelen. |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 1 slice 4 (staging): waarde-normalisatie aan de bron. Nieuw `app/services/ingest/normalize.py`: datums -> `dataquality.parse_date` (ISO), naam-gedetecteerde datumkolommen via `detect_date_fields` (standaard-onafhankelijk); codelijst verzuimsoort -> `normalize_verzuimtype` op Algemeen-velden met type 'verzuimtype'. Vult alleen `CanonicalCell.value`; `raw` en `to_legacy_rows()` blijven ongewijzigd (validators draaien identiek door). Pipeline roept `normalize_file()` aan. 5 tests (datumformaten, ongeldig/leeg blijft staan, value genormaliseerd terwijl raw/legacy gelijk blijft). Suite 227 groen, staging-deploy groen. HIERMEE ZIJN ALLE VIER SLICES VAN STAP 1 KLAAR: canoniek model gevuld met source_type, record_type, field_concepts en genormaliseerde value. VOLGENDE: de omslag (validators op canonical laten draaien) = Stap 2 (generieke controles op het canonieke model + KIK-V/ZIB als declaratieve profielen). |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 1 slice 3 (staging): concept-mapping façade. Nieuw `app/services/ingest/concepts.py` met `map_concepts(standard, record_type, headers) -> {bronkolom: concept}` — delegeert naar de bestaande mapping (KIK-V `auto_map`+col_aliases, ZIB `_auto_map`+field_rules, Algemeen directe header==veld), dus pariteit per constructie. `CanonicalFile` kreeg `field_concepts` + `concept_for()`; pipeline vult die bij een herkend bestand (concept per kolom op bestandsniveau, geen dure loop over rijen). 6 pariteitstests (façade == legacy mapping per pad, onbekend->{}). Suite 222 groen, staging-deploy groen. VOLGENDE: slice 4 (normalisatie naar de bron: datum/codelijst één keer bij ingest, validators lezen canonical .value). |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 1 slice 2 (staging): bronherkenning-façade. Nieuw `app/services/ingest/sources.py` met `detect_source(filename, headers, standard) -> SourceMatch` — één centrale ingang die delegeert naar de bestaande detectoren (`detect_schema` KIK-V, `detect_zib_schema` ZIB, `_detect_template` Algemeen), dus pariteit per constructie. Pipeline (`to_canonical`/`ingest`) accepteert nu `standard` en vult `CanonicalFile.source_type`/`record_type`; zonder `standard` blijft slice-1-gedrag (None). 7 pariteitstests (façade == legacy op representatieve bestanden, incl. onbekend->None). Suite 216 groen, staging-deploy groen. VOLGENDE: het echt samenvoegen van de detectie-metadata tot één register, en slice 3 (concept-mapping centraliseren). |
| 2026-07-06 | — | DOELARCHITECTUUR Stap 1 slice 1 (staging): canoniek rijmodel + ingest-pipeline als fundament. Nieuw `app/services/ingest/` met `canonical.py` (CanonicalCell/CanonicalRow/CanonicalFile, provenance source_column+raw, verliesvrije round-trip via to_raw/to_legacy_rows) en `pipeline.py` (to_canonical/ingest, bewust ontkoppeld van parse_upload/FastAPI → geen circulaire import). Puur fundament: géén validator omgezet, géén gedragswijziging; source_type/record_type (bronherkenning), concept (mapping) en value (normalisatie) volgen in slice 2-4 (nu value==raw, concept=None). 7 pariteitstests (canonical→to_legacy_rows identiek aan csv/xml/json parsers, incl. behoud partiële sleutels). Suite 209 groen, staging-deploy groen. Besluiten Index v2.0 vastgelegd in `Rhadix_Index_v2_besluitenblad.docx`; plan in `STAP1_implementatieplan.md`. VOLGENDE: slice 2 (bronherkenning centraliseren: detect_schema/detect_zib_schema/algemeen → SOURCE_REGISTRY, met pariteitstests). |
| 2026-06-30 | — | Platform-admin naar echt adres (staging). `_ensure_admin()` is nu env-gestuurd (`RHADIX_ADMIN_EMAIL`/`RHADIX_ADMIN_PASSWORD`) met **niet-destructieve hernoeming**: bestaand `admin@rhadix.nl` wordt hernoemd i.p.v. een tweede admin aanmaken; wachtwoord wordt niet meer bij elke herstart geforceerd (alleen als het ontbreekt of via env). mailer kreeg `SMTP_REPLY_TO`-hook. docker-compose staging+prod geven `RHADIX_ADMIN_EMAIL/PASSWORD`, `SMTP_REPLY_TO` en `AUTH_*_TTL` door aan de backend. Op staging `RHADIX_ADMIN_EMAIL=r.houwen10265@gmail.com` in `/opt/rhadix-app/mail.env` gezet + redeploy → geverifieerd: login met gmail = 200, oud admin@rhadix.nl = 401 (hernoemd, geen duplicaat). **E-mailinfra-bevinding:** Scaleway TEM is verzenden-only (MX = blackhole, geen inbox); rhadix.nl heeft nu geen postbus. Route voor echte `info@`/`support@`: via M365 van Rhoderlandengroep (domein toevoegen, gedeelde postbussen, MX→Microsoft, SPF samenvoegen `_spf.tem.scaleway.com`+`spf.protection.outlook.com`, M365-DKIM). Zodra postbussen bestaan: `SMTP_REPLY_TO=support@rhadix.nl` + `RHADIX_ADMIN_EMAIL` naar rhadix.nl-adres. Testaccounts (demo1@ + 3 rhoderlandengroep) blijven staan op verzoek. TODO: e-mailinfra (Rene), daarna auth-flows + admin-adres porten naar prod. |
| 2026-06-30 | — | AUTH E-MAILFLOWS op de bestaande mailserver aangesloten (staging, Datavalidatie). Vier flows: **wachtwoord-reset** (self-service vanaf login, geen account-enumeratie — altijd 204), **uitnodiging nieuwe gebruiker** (admin maakt user zonder wachtwoord aan → activatiemail met set-password-link; user `email_verified=false` tot activatie), **e-mailverificatie**, en **wachtwoord-gewijzigd-bevestiging** (bij reset én change_password). Backend: nieuw `AuthToken`-model (purpose reset/invite/verify; alleen SHA-256-hash opgeslagen, eenmalig `used_at`, expiry via env AUTH_RESET_TTL_MIN=60/AUTH_INVITE_TTL_DAYS=7/AUTH_VERIFY_TTL_HOURS=24) + `users.email_verified`; Alembic 0005 + startup-vangnet `_ensure_auth_tokens` (ALTER ADD COLUMN IF NOT EXISTS + create checkfirst). `security.generate_url_token/hash_url_token`. `mailer`: 4 nieuwe functies, `_shell` generiek gemaakt (cta-label + footer), dataminimaal (alleen naam + actie + link). Endpoints publiek/token-gebaseerd: POST /auth/{forgot-password,reset-password,set-password,verify-email}. `admin_create_user` kreeg `send_invite`. Links via PUBLIC_BASE_URL (`/?action=…&token=…`). Frontend: LoginScreen 'wachtwoord vergeten' = echte resetlink-aanvraag; nieuwe `AuthAction`-pagina (reset/invite/verify) met wachtwoordeisen-indicator; App routet op `?action=&token=` vóór de auth-guard; api.js-helpers. 10 nieuwe tests; **suite 202 groen**. Endpoints live geverifieerd op staging (204/400/400). TODO: e2e inbox-test door Rene; daarna prod-release. |
| 2026-06-30 | — | Wit scherm na fase 1 bij Algemeen-resultaat (o.a. AFAS `Medewerkergegevens.json`) opgelost. **Directe bug:** `AlgemeenDashboard.jsx` gebruikte `<BenchmarkBar>` zonder import → React 'Element type is invalid' = wit scherm (ZibDashboard had de import wél). **Venijnig:** staging serveerde een *stale frontend-bundle* — asset-hash bleef twee pushes lang op `Do_6REKz` hangen, dus de fix werd niet uitgeleverd; pas na een derde push brak de GHA-frontend-buildcache (`cache-from gha,scope=frontend`) open (`Do_6REKz`→`CDz_QHTE`). Les: na frontend-fix altijd de live asset-hash controleren, niet alleen 'deploy success'. **Vangnet toegevoegd:** `ErrorBoundary` (components/ErrorBoundary.jsx) rond `<App/>` in main.jsx — render-fout geeft nu een leesbare melding + stacktrace i.p.v. blanco scherm. Tevens `algemeen_validator` robuust voor JSON-bronnen: numerieke/boolean waarden naar string coercen vóór formaatvalidatie (`(val or '').strip()` crashte op float). Alles op staging. |
| 2026-06-30 | v1.7.2 | RELEASE-voorbereiding: Stap 0 doelarchitectuur naar productie. Bevat de gedeelde datum-primitief `dataquality.py` (één `is_date`/`parse_date` over KIK-V/Algemeen/ZIB/OWL), de zichtbare+verhoogde rij-cap (RHADIX_MAX_ROWS, default 100000, met TruncationWarning-banner) en de verzuimsoort-codelijst in Algemeen. Lost Noorderboog TB-002/004/006/007 structureel op. 188 tests groen. Tag v1.7.2 -> prod-deploy via GitHub Actions (handmatige goedkeuring). |
| 2026-06-30 | — | STAP 0 COMPLEET op staging. Slice 3: ZIB (`_is_date_like`) en OWL (`_parse_date`/`_DATE_FMTS`) ingevouwen in de gedeelde `dataquality.is_date` — datumlogica nu in ÉÉN module over alle vier de sporen (KIK-V, Algemeen, ZIB, OWL). Algemeen kreeg een `verzuimtype`-validator (hergebruikt `rules.normalize_verzuimtype`+`VERZUIMTYPE_VALUES`); AbsenceTypeId wordt nu tegen de codelijst gevalideerd -> gewijzigde/onbekende verzuimsoort wordt gesignaleerd (Noorderboog TB-006). 3 nieuwe tests; suite 188 groen. Hiermee zijn ALLE Noorderboog-bevindingen TB-002/004/006/007 structureel opgelost en is Laag 1 (gedeelde primitieven) van de doelarchitectuur neergezet. Alles op staging-branch; nog niet op main/prod. TODO: testen op staging -> daarna release naar prod (nieuwe tag) als akkoord. Aandachtspunt: AFAS_VERZUIM_*_MAP uitbreiden zodra onbekende-maar-echte AFAS-codes opduiken (voorkomt false positives op verzuimsoort). |
| 2026-06-30 | — | STAP 0 slice 2 (staging): rij-cap zichtbaar + verhoogd (Noorderboog TB-002/004). MAX_ROWS configureerbaar via RHADIX_MAX_ROWS (2000 -> default 100000) zodat normale HR-exports (3105/11561 rijen) niet meer stil worden afgekapt. Parsers geven nu de échte totaalcount terug (3-tuple); upload-endpoint zet een `truncation`-blok in de response; frontend toont een TruncationWarning-banner (X van Y rijen verwerkt) op de dashboards. Suite 185 groen, frontend-build schoon. Staat op staging. Resteert van Stap 0: ZIB/_is_date_like + OWL/_parse_date invouwen in dataquality; verzuimsoort-codelijst in Algemeen (TB-006). |
| 2026-06-30 | — | STAP 0 doelarchitectuur gestart (staging). Eerste slice: gedeelde validatie-primitief `app/services/dataquality.py` met één `parse_date`/`is_date`. KIK-V (`validator.py`) en Algemeen (`algemeen_validator._valid_date`) gebruiken die nu — datumlogica niet meer gedupliceerd. Lost Noorderboog TB-007 structureel op: is_date accepteert jaar-eerst/ISO + kort yyyymmdd (KIK-V keurde alle AFAS-datums onterecht af), en geldigheid = echte kalenderdatum (Algemeen miste niet-bestaande datum als 2026-02-30). 5 nieuwe tests; suite 184 groen. Staat op staging-branch (nog niet op main). VOLGENDE slices Stap 0: rij-cap (MAX_ROWS=2000) zichtbaar/luid maken (TB-002/004), ZIB `_is_date_like` + OWL `_parse_date` invouwen in dataquality, verzuimsoort-codelijst in Algemeen (TB-006). |
| 2026-06-30 | CRM-data | RSO-verrijking RSO 2/17 — RijnmondNet (regio Rijnmond) bestuursniveau compleet. Koepelstructuur (ziekenhuizen via SRZ=10, GGZ Parnassia/Antes+Yulius, huisartsen Rijnmond Dokters, lab Star-shl). 14 niet-VVT-orgs (9 ZKH, 2 GGZ, revalidatie+lab=OVERIG, HA-koepel) + 18 bestuurscontacten, bron+zekerheid. CRM nu 229 orgs / 363 contactpersonen. Resteren 15 RSO's. RSO-structuren verschillen sterk -> per RSO maatwerk. |
| 2026-06-30 | CRM-data | RSO-ledenverrijking gestart (niet-VVT-aanbieders). Sectorcategorieën gereleased (v0.1.5: soort ZKH/GGZ/GHZ/HA/OVERIG + Relaties-filter/badges + dashboard per sector). Onderzoek via Claude-in-Chrome (JS-gerenderde ledenpagina's) + WebSearch op openbare bronnen. **RSO 1/17 — Sigra VOLLEDIG op bestuursniveau:** 46 niet-VVT-organisaties geladen (11 ziekenhuizen, 16 GGZ, 12 GHZ, 7 huisartsenzorg) + 64 bestuurs-/directiecontacten (rolniveau Bestuur/directie) met bron_url + zekerheid. CRM nu 215 orgs / 345 contactpersonen. Discipline: alleen openbare gratis bronnen, géén persoonlijke e-mails (NULL), géén gokwerk; 3 orgs zonder eenduidige bestuurder bewust NULL (De Waerden, Fokus Wonen, Perspectief). OPEN: IT/CIO-contacten (aparte ronde, vaak niet openbaar) + 16 resterende RSO's (NOEL, REN West-Brabant, RSO Haaglanden, RSO NO-Brabant, RSO Zuid-Limburg, RZCC, RijnmondNet, Salland United, Samen Gezond IJssel-Vecht, Sleutelnet, Trijn, VDZA, ZorgNetOost, Zorgnetwerk MB, Zorgring). Data staat in prod-DB, niet in git. |
| 2026-06-29 | v1.7.1 | DV-prod-mail gerepareerd. Gedeelde `/opt/rhadix-app/mail.env` werd door zowel prod- als staging-deploy ingelezen, met daarin `PUBLIC_BASE_URL=app-staging.rhadix.nl` -> prod-taakmails kregen de staging-link. Opgelost als bij CRM: `PUBLIC_BASE_URL` per omgeving als compose-default (prod=https://app.rhadix.nl, staging=https://app-staging.rhadix.nl) en de regel uit de gedeelde mail.env verwijderd (backup mail.env.bak op server). Tag v1.7.1 -> prod-deploy groen; /api/health prod = Rhadix Validator ok. Beide omgevingen linken nu correct. |
| 2026-06-29 | CRM live | CRM-productie compleet + e-mail end-to-end bevestigd. v0.1.3 én v0.1.4 prod-deploys groen (GitHub Actions). `/opt/crm-app/mail.env` aangemaakt (kopie van DV-creds, ZONDER PUBLIC_BASE_URL — compose zet die per omgeving); deploy v0.1.4 her-gedraaid zodat mail.env is ingelezen. Geverifieerd via API: /api/tasks live (200); testtaak toegewezen aan 2e gebruiker -> taakmail daadwerkelijk ontvangen (link naar crm.rhadix.nl) -> testtaak weer verwijderd. RESTEREND: Datavalidatie-prod-mail — `/opt/rhadix-app/mail.env` heeft nog `PUBLIC_BASE_URL=app-staging.rhadix.nl`; voor prod naar de DV-prod-URL zetten (let op of dit bestand ook door DV-staging wordt gebruikt). |
| 2026-06-29 | CRM v0.1.4 | Taken-/workflowmodule + mailserver naar CRM-PRODUCTIE. Mail-env toegevoegd aan CRM docker-compose (prod+staging) en deploy-workflows lezen `/opt/crm-app/mail.env` in (zoals DV). PUBLIC_BASE_URL per omgeving via compose-default (prod=crm.rhadix.nl, staging=crm-staging.rhadix.nl); SMTP/MAIL_ENABLED default UIT. tasks-tabel komt bij deploy via bootstrap `create_all`. staging->main gemerged, tag v0.1.4 (deploy wacht op goedkeuring). 24 tests groen. OPEN bij Rene: `mail.env` plaatsen op server voor zowel CRM (`/opt/crm-app/mail.env`) als Datavalidatie (`/opt/rhadix-app/mail.env`) met de Scaleway-SMTP-creds; CRM hoeft GEEN PUBLIC_BASE_URL in mail.env (compose regelt per omgeving). |
| 2026-06-29 | — | CRM-data naar PRODUCTIE gemigreerd (via CRM-API, idempotent script `scripts/crm_data_migratie.py`). 169 organisaties verrijkt (website/plaats/KvK/e-mail/LinkedIn/bron/betrouwbaarheid; 162 met website/kvk/plaats — 7 bewust leeg zonder openbare bron) en 219 contactpersonen aangemaakt (prod 68->287). Match op organisatienaam; accounthouder_id bewust niet meegenomen (UUID's verschillen per omgeving). Cloudflare blokkeerde kale urllib (fout 1010) -> browser-User-Agent toegevoegd. Inloggegevens via env, niet in git. OPEN bij Rene: prod-mail Datavalidatie (mail.env), en deploy-goedkeuringen v1.7.0/v0.1.3 in GitHub Actions controleren. |
| 2026-06-29 | CRM v0.1.3 | CRM gelijktrekken + taken-module geport. (1) CRM staging->main gemerged (schone auto-merge, SSO-prodsleutel CENTRAL_JWT_PUBLIC_KEY behouden): genereer-krachtenveld-knop + KvK/plaats + e-mail/LinkedIn/accounthouder naar prod; tag v0.1.3 (deploy wacht op goedkeuring). 18 tests groen. (2) Taken-/workflowmodule + generieke mailer vanuit Datavalidatie geport naar CRM op staging-branch: task_models met dialect-neutraal GUID-type (CRM heeft geen Alembic -> tabel via bootstrap Base.metadata.create_all), /api/tasks-router (rol RHADIX_ADMIN->PLATFORM_ADMIN, app_slug=rhadix-crm), Taken-tab+pagina (scope mine/created/all, prioriteit/toewijzen/status/deadline) + API-functies. 6 nieuwe tests; CRM-suite 24 groen, frontend-build schoon. OPEN: CRM-data staging->prod migreren (verrijking zit in staging-DB; via CRM-API herladen — wacht op staging+prod login/URL's); prod-mail Datavalidatie activeren (mail.env met prod PUBLIC_BASE_URL op server); v1.7.0 (DV) + v0.1.3 (CRM) prod-deploys goedkeuren in GitHub Actions. Ook opgeleverd: functioneel Word-document 'Rhadix_Index_validatiechecks.docx' (validatiechecks + Rhadix Index-formules) voor externe review. |
| 2026-06-29 | v1.7.0 | RELEASE — staging en productie gelijkgetrokken. `staging`->`main` gemerged (schone auto-merge, geen conflicten): main krijgt de taken-/workflow-module + Scaleway-mailer + AFAS-fixes (openpyxl `read_only`-fallback, robuuste datumparser zonder dag/maand te gokken, `normalize_verzuimtype`); SSO-prodconfig (RS256/JWT, `SSO_COOKIE_DOMAIN=.rhadix.nl`) én mail-config blijven beide behouden in `docker-compose.prod.yml` + `deploy-production.yml`. staging daarna fast-forward gelijkgetrokken aan main (branches identiek). 179 tests groen. Tag v1.7.0 -> prod-deploy via GitHub Actions (handmatige goedkeuring). NB: prod-mail pas actief na zetten `mail.env` met prod `PUBLIC_BASE_URL` (zie Taken_email_activatie_Scaleway.md). |
| 2026-06-26 | — | VOLGENDE STAPPEN (nu echte e-mail werkt): (1) generieke "wachtwoord vergeten"-flow — reset-link via mail met aflopende token, herbruikbaar over de 4 apps; (2) gebruikersbeheer met ECHTE adressen + uitnodigings-/welkomstmail ("stel je wachtwoord in"-link i.p.v. handmatig ww door beheerder); (3) nep/placeholder-e-mailadressen opruimen/vervangen door echte. LET OP bij (3): eerst OVERZICHT placeholder vs echt maken; admin@rhadix.nl en bestaande ECHTE accounts NIET raken (admin-login moet blijven werken); demo-accounts achter demo-vlag laten of bewust opruimen; AVG dataminimaal. Reeds open: CRM-port takenmodule (#48) en prod-release taken+CRM-verrijking. |
| 2026-06-26 | — | TAKEN/WORKFLOW-MODULE + e-mailnotificaties (staging, Datavalidatie). Generieke, app-onafhankelijke takenlijst op gebruikersniveau: nieuwe `tasks`-tabel (tenant-gescoped; assignee/created_by→users; status/prioriteit/deadline; source_type/ref/label voor koppeling AFAS/CRM). `/api/tasks` router (lijst mine/created/all, summary-badge, assignable-users, create, bulk, patch, delete) — toewijzen alleen binnen eigen organisatie; ORG_ADMIN ziet via tabblad 'Hele organisatie' alles binnen de tenant, gewone gebruiker alleen eigen toegewezen/aangemaakt; strikt tenant-gescoped (ook RHADIX_ADMIN). Frontend: 'Mijn taken'-widget + knop op Landing (alleen ingelogd), volwaardige takenpagina, en herbruikbare 'Maak taken van bevindingen'-knop op AFAS-resultaat (Algemeen + KIK-V; bulk → taken, errors=prioriteit Hoog). E-MAIL: generieke env-gestuurde SMTP-mailer (`app/services/mailer.py`, default UIT), toewijzingsmail bij create/bulk/herverdeling (alleen naar een ander; dataminimaal: enkel taaktitel + toewijzer + inloglink, géén zorgdata). Leverancier **Scaleway Transactional Email** (EU-soeverein/Parijs, ISO 27001, AVG-DPA) — rhadix.nl geverifieerd (SPF/DKIM/MX in Cloudflare; bestaande strikte DMARC behouden). Mail-config via persistent `/opt/rhadix-app/mail.env` (door deploy-workflow ingelezen, overleeft redeploys; compose geeft MAIL_*/SMTP_*/PUBLIC_BASE_URL door aan backend). Fixes onderweg: tasks-tabel ontbrak (Alembic-migratie stil mislukt → startup-vangnet `_ensure_tasks_table` checkfirst); enum→native_enum=False (Postgres 500 bij insert); compose-projectnaam `-p rhadix-staging` nodig bij handmatige `up`. 179 tests groen. Mail getest: komt binnen. TODO: prod-activatie (zelfde mail.env met prod PUBLIC_BASE_URL) en module porten naar CRM. |
| 2026-06-26 | — | CRM-dataverrijking VOLTOOID (staging): alle 17 RSO's + 152 VVT-aanbieders verrijkt met website/plaats/KvK/gepubliceerde e-mail + bestuurders (raad van bestuur/directie) als contactpersoon. Totaal 293 contactpersonen met bron-URL en zekerheid (202 Hoog / 49 Middel / 41 Laag). Discipline: uitsluitend openbare bronnen (eigen bestuurspagina's > vakmedia), géén afgeleide e-mails, géén gokwerk, NULL waar niets gevonden. 151/152 VVT met website ('Lante' = geen bron, vermoedelijk typefout in bronlijst). Geladen via API (PATCH org + POST contactpersonen, idempotent). Datakwaliteit-flags vastgelegd in `bron_opmerking`/`opmerking`: vermoedelijke dubbelingen over regio's (Thebe, TanteLouise, Zorgwaard, Lelie zorggroep, Zorgspectrum Het Zand, IJsselheem=Woonzorgconcern IJsselheem, ZGR=Zorggroep Raalte), vermoedelijk geen VVT (MediReva, ONS welzijn, WijZijn Traverse Groep), bestuurder bewust NULL bij vacature/niet-openbaar (Daelzicht, Joris Zorg, Lunet, Ananz, Zorg in Oktober, De Posten, Radar), Cicero-naam 'Midden-Limburg' onjuist (Brunssum). Topaz-regel (E. Kalbfleisch) gecorrigeerd na constatering overstap naar De Zorgcirkel. Verificatie-overzicht: `Rhadix_CRM_verificatielijst.xlsx`. NB: verrijking staat in de staging-DB (data, niet in git). |
| 2026-06-24 | — | SSO op productie werkend: RS256-token over alle 4 apps. JWT_PRIVATE_KEY was leeg (lege secret); opgelost via persistent `/opt/rhadix-app/jwt_private.env` + deploy-workflow leest dat bestand. Geverifieerd: uitvraag/datastation/crm geven 200 op /api/auth/me met centraal token. |
| 2026-06-24 | v1.6.2/3 · u v0.7.3 · ds v1.0.3 · crm v0.1.2 | SSO-bedrading op productie uitgerold over alle 4 apps (RS256-uitgever datavalidatie + resource-apps + SSO-cookie .rhadix.nl). OPEN: JWT_PRIVATE_KEY leeg op datavalidatie-prod → token nog HS256 → alleen CRM-SSO werkt; morgen fixen (zie OPENSTAAND-sectie). Vers prod-sleutelpaar; publieke sleutel als default in 4 prod-composes. |
| 2026-06-23 | v1.6.1 | FIX: CRM-tegel actief op prod-portal (CRM_ACTIVE=true; in v1.6.0 stond per ongeluk !IS_PROD door ongestagede sed). |
| 2026-06-23 | v1.6.0 | RELEASE flow+marketing naar prod: gestripte Rhadix-Index-landing (PlatformLanding) + kaart-grid-portal op app.rhadix.nl; CRM als 4e tegel (crm.rhadix.nl). Marketing rhadix.nl (/var/www/rhadix/index.html): Inloggen->Rhadix platform, Probeer-knoppen weg, Klaar om te starten?. SureSync-merklaag + centrale identiteit meegekomen maar omgevings-gegated (dormant op prod). 159 tests groen. |
| 2026-06-23 | v1.5.28/29 | Admin-wachtwoord platformbreed -> Rhadixvoordezorg26! (DV v1.5.28 + v1.5.29 _ensure_admin dwingt af op bestaande admin; Datastation v1.0.2, Uitvraag v0.7.2, CRM v0.1.1). Nieuwe app Rhadix-crm live (crm.rhadix.nl + crm-staging). nginx live-config = /etc/nginx/sites-enabled/rhadix (aparte kopie, GEEN symlink); marketing = static /var/www/rhadix. |
| 2026-06-23 | — | Platform-flow conform 'wijzigingen flow.docx': scherm 1 = gestripte Datavalidatie-landing (hero + Rhadix Index-teller, alleen Inloggen, geen scan/reconciliatie/stappen); scherm 2 = portal als kaart-grid (3 app-tegels) met gedeelde Nav Dashboard/Beheer (Nav-knop 'Admin'->'Beheer'). OPEN: via Beheer moet admin org-beheerders+gebruikers toevoegen en apps koppelen via de licentiemodule — wacht op spec van Rene's collega. Alles op staging. |
| 2026-06-19 | — | Platform-flow: 'Rhadix platform'-entree = PlatformLanding met de **Rhadix Index** centraal + de drie applicaties 'in dienst van de index' + Inloggen. Na inloggen → portal (AppPortal) in Validatie-stijl met **Dashboard/Beheer/Terug**-nav. Marketingsite-CTA's vereenvoudigd naar 'Rhadix platform'. Unified identity (SureSync ID, RS256/JWKS/SSO) staat actief op staging (app-/uitvraag-/datastation-staging.rhadix.nl). Alles staging. |
| 2026-06-18 | — | Fase 1 stap 1b+1c: Uitvraag+Datastation accepteren centraal SureSync ID-token (RS256 via CENTRAL_JWT_PUBLIC_KEY of cookie) met JIT-provisioning + rol-mapping; lokale login blijft fallback. Centraal: product-Applications (datavalidatie/uitvraag/datastation) idempotent geseed (`_ensure_apps`) voor centrale toegangssturing. Identiteit brand-agnostisch (Rhadix+SureSync). Aanzetten = RSA-sleutels in env + gedeeld domein (*.rhadix.nl). Suites groen. |
| 2026-06-18 | — | Fase 1 centrale identiteit (SureSync ID), stap 1: token SSO-klaar gemaakt. Verrijkte claims (tenant_name, name, email, apps, iss). RS256/JWKS **optioneel** (env JWT_PRIVATE_KEY/JWT_PUBLIC_KEY; anders HS256 — bestaand gedrag intact), nieuw endpoint GET /api/auth/jwks. SSO-cookie 'rhadix_sso' op gedeeld domein als SSO_COOKIE_DOMAIN gezet is (bv. .rhadix.nl). Alles additief; 159 tests groen. Activeren = RSA-sleutels + SSO_COOKIE_DOMAIN in deploy-env zetten. |
| 2026-06-18 | — | White-label merk-laag (staging-only): `brand.js` (rhadix default + suresync), `index.css` `:root[data-brand=suresync]` provisorisch teal-palet, App.jsx brand-state (URL `?brand=`/sessionStorage) zet `data-brand` op <html>, AppPortal merk-bewust logo/sub + 'SureSync'-knop (gegate op VITE_RHADIX_ENV != production). Volledig omkeerbaar; productie ongewijzigd. SureSync-kleuren/logo nog VOORLOPIG (wachten op brand guide). |
| 2026-06-18 | v1.5.27 | RELEASE: AFAS-herkenning Werkgevers/Functies/Organigram (3 templates toegevoegd), geslacht 'X' toegestaan, en navy-palet-herstel meegenomen naar productie. 157 tests groen. |
| 2026-06-18 | — | AFAS-import fix: 3 ontbrekende connectoren toegevoegd aan AFAS_TEMPLATES (employers/Werkgevers, functions/Functies, organisation/Organigram). Die gaven 'Bestandstype niet herkend'. Herkenning nu ook op officiële `GetConnector_Profit_*`-namen + header-signature. Velden geverifieerd tegen aangeleverde AFAS-templates (5/6 identiek; Organigram-template was foutief een kopie van Werkgevers — echte org-chart-velden gebruikt). 156 tests groen. |
| 2026-06-17 | v1.5.26 | RELEASE: palet-regressie hersteld — productie weer navy (groen alleen op staging via data-env). |
| 2026-06-17 | — | Fix palet-regressie: door eerdere `-X theirs` staging->main merges was het staging-groene palet op productie beland. index.css nu omgevings-gestuurd: `:root` = navy (prod), `:root[data-env=staging]` = salie groen; main.jsx zet data-env uit VITE_RHADIX_ENV. Identiek op beide branches -> merge kan palet niet meer wisselen. |
| 2026-06-17 | v1.5.25 | RELEASE: Datastation-knop op productie geactiveerd -> https://datastation.rhadix.nl (Datastation kreeg eigen prod-deploy v1.0.0 + nginx-vhost 5180/8016 + Cloudflare DNS). Tevens portal staging Datastation-URL gecorrigeerd 5176->5181 (5176 was Uitvraag-prod). |
| 2026-06-17 | v1.5.24 | RELEASE naar productie: staging -> main gemerged. Bevat: AFAS GetConnector JSON-import (parse_json_bytes), niet-destructieve auth-bootstrap (geen TRUNCATE), werkende demo-login achter DEMO_SEED/staging, Beheer-knop op landing, portal-volgorde DV/Uitvraag/Datastation met omgevings-afhankelijke URL's (Datastation prod='Binnenkort'). 153 tests groen. |
| 2026-06-17 | — | Portal-URL's omgevings-afhankelijk via VITE_RHADIX_ENV: Uitvraag prod=https://uitvraag.rhadix.nl / staging=:5177; Datastation staging=:5176 actief, prod='Binnenkort' (Rhadix-datastation heeft nog geen prod-release: geen tags). Voorbereiding release v1.5.24. |
| 2026-06-17 | — | Demo achter eigen vlag: backend `_ensure_demo_user` nu gegate door `DEMO_SEED` (expliciet wint; default alleen seeden als `RHADIX_ENV=staging`) i.p.v. AUTH_RESET. Frontend: 'Demo toegang'-blok op loginscherm alleen tonen buiten productie (VITE_RHADIX_ENV != production). Prod blijft schoon, staging houdt demo. env-examples: DEMO_SEED gedocumenteerd. Suite 153 passed. |
| 2026-06-17 | — | Demo + beheer: demo-login werkend gemaakt — `_ensure_demo_user` seedt idempotent demo1@rhadix.nl (ORG_ADMIN) in tenant 'rhadix-demo' met app-toegang (TenantApplication+UserApplication voor alle actieve apps). Beheer-knop toegevoegd aan Landing-header (RHADIX_ADMIN->Admin/AdminDashboard, ORG_ADMIN->OrgAdminDashboard); header negeerde onAdmin/onOrgAdmin eerder. Suite 153 passed. |
| 2026-06-17 | — | Portal (AppPortal.jsx): volgorde Datavalidatie -> Uitvraag -> Datastation, alle knoppen 'Inloggen', Datastation geactiveerd (env VITE_DATASTATION_URL, fallback poort 5176, bevestigd door Rene). Uitvraag/Datastation-URL per omgeving via VITE_*. |
| 2026-06-17 | — | Alignment + security: destructieve `_reset_single_admin` (TRUNCATE users + hardcoded admin elke herstart) vervangen door niet-destructieve `_ensure_admin` (admin geborgd, gebruikers behouden). AppPortal Uitvraag-knop label 'Openen'->'Inloggen' op staging (gelijk aan main; URL blijft env-specifiek). Token uit git-remote verwijderd. Repo-divergentie in kaart: staging 22 vóór op main, main 2 (Uitvraag-knop + testopzet). Plan: staging=bron -> release naar prod na test. Suite 153 passed. |
| 2026-06-17 | — | Import-kant: JSON-parser (`parse_json_bytes`) toegevoegd voor AFAS GetConnector-formaat `{skip,take,rows}` + losse dict/array; `json` toegevoegd aan parse_upload, extensie-allowlist en frontend accept-filters (Upload.jsx, ReconciliationDashboard.jsx). 5 nieuwe tests incl. XML/JSON-pariteit; pariteit bevestigd op 6 echte connector-bestanden (Werkgevers, Organigram, Functies, Medewerkergegevens, roosters, verzuimverloop). Volledige suite 153 passed. Repo-URL's (3) vastgelegd. |
| 2026-06-09 | — | Reconciliation Engine — "SPARQL loslaten op de data": nieuwe `rdf_store.py` (kolom→concept mapping → RDF-triples → triple store). **Fuseki** als triple store toegevoegd aan alle docker-compose-bestanden (stain/jena-fuseki:5.1.0, -Xmx512m, intern netwerk) met **rdflib in-memory fallback**. Nieuwe endpoints: GET /concepts, POST /preview-columns, POST /sparql-reconcile. Frontend: SparqlOnDataPanel in manual-tab (kolom→concept mapping-UI, record-class + ID-kolom keuze, triples genereren + SPARQL draaien, resultaat + vergelijking met berekeningsregel). XML-fix in Happy Flow drag-drop. Env-var FUSEKI_PASSWORD/STAGING_FUSEKI_PASSWORD. |
| 2026-05-26 | — | Reconciliation Engine: happy flow batch-feature gebouwd. 24 YAML-regels voor alle CSV-typen (medewerker, werkovereenkomst, client, verzuim, financieel, vestiging, functie, kostenplaats). Nieuw endpoint POST /api/reconciliation/happy-flow/batch + GET /happy-flow/rules. Frontend: tabblad "Happy Flow batch" met multi-file upload, auto-detectie op bestandsnaam, SPARQL-koppeling vanuit uitwisselprofiel. DayFirst=True fix voor dd/MM/yyyy datumnotatie. |
| 2026-05-22 | v1.5.15 | SVG logo vervangen door JPG brand assets (logo + boom) in UI.jsx, Landing.jsx, LoginScreen.jsx |
| 2026-05-22 | — | Productie hersteld na bad gateway: .env.production ontbrak, GHCR_ORG miste |
| 2026-05-22 | — | Server gestabiliseerd: 2GB swap toegevoegd, Docker-on-boot bevestigd, restart:always aanwezig |
| 2026-05-23 | — | Productie hersteld (502), swap-persistent fix, deploy workflow verbeterd met automatische rollback |
| 2026-05-22 | v1.5.19 | Terug-knop (→ login) + Dashboard-knop volgorde in nav landing page |
| 2026-05-22 | v1.5.18 | Terug naar rhadix.nl knop toegevoegd aan nav |
| 2026-05-22 | v1.5.17 | NavBack import fix UserDashboard (wit scherm), staging groen kleurenpalet hersteld |
| 2026-05-22 | v1.5.16 | Staging gereset naar main, pipeline hersteld (staging=groen, prod=navy) |
| 2026-05-22 | — | CLAUDE.md aangemaakt als persistent projectgeheugen |
| 2026-05-21 | v1.5.14 | BIO security hardening (B01-B10) voltooid |
